import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hey Seeker! | AI Assistant for Solana Seed Vault",
  description: "Hey Seeker! is an innovative AI assistant that directly interacts with the Solana phone's Seed Vault for seamless transactions.",
  keywords: ["Hey Seeker", "Solana", "Web3", "AI", "Seed Vault", "Solana Phone", "Blockchain", "Crypto"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
